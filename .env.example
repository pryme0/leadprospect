NEXT_PUBLIC_API_URL=http://localhost:3003

# Google Tag Manager container ID. Loads the GTM container into <head>
# at build time — NEXT_PUBLIC_* vars are baked into the client bundle by
# Next.js, so you must rebuild the frontend after changing this.
NEXT_PUBLIC_GTM_ID=GTM-W9NFPZZ

# Amplitude Browser SDK API key. Get this from Amplitude →
# Sources → emc_web. Loaded on every page via the root layout.
# NEXT_PUBLIC_* vars are baked into the client bundle, so rebuild
# the frontend after changing this.
NEXT_PUBLIC_AMPLITUDE_API_KEY=c6fb9735183d0aef6a49255d2d81c715

REDDIT_BRAND_SUBREDDIT=excelmindcyber